import { render } from '@testing-library/react';

import Checkbox from '../checkbox';

describe('Checkbox', () => {
  it('should render correctly', () => {
    render(<Checkbox />);
    // ...
  });
});
