import { render } from '@testing-library/react';

import FileInput from '../file-input';

describe('File Input', () => {
  it('should render correctly', () => {
    render(<FileInput />);
    // ...
  });
});
