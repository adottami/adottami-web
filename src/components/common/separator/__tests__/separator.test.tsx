import { render } from '@testing-library/react';

import Separator from '../separator';

describe('Separator', () => {
  it('should render correctly', () => {
    render(<Separator />);
    // ...
  });
});
