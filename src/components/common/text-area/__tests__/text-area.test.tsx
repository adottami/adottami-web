import { render } from '@testing-library/react';

import TextArea from '../text-area';

describe('Text area', () => {
  it('should render correctly', () => {
    render(<TextArea />);
    // ...
  });
});
