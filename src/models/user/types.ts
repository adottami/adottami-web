export interface UserResponse {
  id: string;
  name: string;
  email: string;
  phoneNumber?: string;
}

export interface UserFields {
  id: string;
  name: string;
  email: string;
  phoneNumber?: string;
}
