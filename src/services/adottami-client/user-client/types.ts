export interface CreateUserData {
  name: string;
  email: string;
  password: string;
  phoneNumber: string;
}

export interface EditUserData {
  name: string;
  email: string;
  phoneNumber: string;
}
